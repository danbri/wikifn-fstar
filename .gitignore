node_modules/
.venv/
coverage/
.DS_Store
build/*
# 1.3 KB, and the only thing standing behind the refusal budgets in
# test/coverage.test.js. Untracked, those eight tests skip wherever the
# generator has not just been run - which is everywhere except the machine that
# ran it, CI included, and a budget that skips is not a budget.
!build/generation-report.json
!build/fstar/
build/fstar/*
!build/fstar/ocaml/
!build/fstar/ocaml/*.ml
!build/fstar/ocaml/*.cmi
!build/fstar/ocaml/*.cmo
!build/fstar/wikifn_primitives_browser.byte
!build/fstar/wikifn_primitives_demo.byte
!build/fstar/wikifn_call.byte
!build/fstar/wikifn_call_browser.byte
tmp/
cache/dumps/
cache/wikifunctions/
cache/*.sqlite
cache/*.sqlite-*
third_party/fstar-z3/
third_party/wikifunctions-dumps/**/*.xml
*.log
*.checked
src/fstar/.cache/

# Personal Claude Code settings; the skills beside them are shared.
.claude/settings.local.json
